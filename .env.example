# Copy to .env and add a key to enable driving distance/time.
# OpenRouteService (tried first): ORS_API_KEY= or OPENROUTE_API_KEY=
# Google (fallback): API_KEY= (Distance Matrix API)
# Leave all unset to use straight-line distance only.
# ORS_API_KEY=
# API_KEY=
